{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
    <h1>Book list</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book.id }}</td>
            </tr>
            <tr>
                <th>ISBN</th>
                <td>{{ book.ISBN }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ book.Autor }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ book.Title }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td> € {{ book.Price }}</td>
            </tr>
            <tr>
                <th>Reviews of users</th>
                {% for review in review %}
                <td> <a href="{{ path('review_show', {'id': review.id}) }}"> {{ review.name }}</a></td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('book_index') }}">back to list</a>

    <a href="{{ path('book_edit', {'id': book.id}) }}">edit</a>

    {{ include('book/_delete_form.html.twig') }}
{% endblock %}
